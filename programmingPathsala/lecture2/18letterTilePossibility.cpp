#include<bits/stdc++.h>
#include <algorithm>
#include <vector>
#include <map>
#include <set>
#include <string>
#include <bitset>
#include <queue>
#include <stack>
using namespace std;
// #define fastio ios_base::sync_with_stdio(false), cin.tie(NULL), cout.tie(NULL)

void solve(int ind,string &s,string &emp){
  int n = s.size();
  if(ind>=n){
    cout<<emp<<endl;
    return;
  }
}

int main() {
  // fastio
  string s = "AAB";
  string emp;
  solve(0,s,emp);
  return 0;
}